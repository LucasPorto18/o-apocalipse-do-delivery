{
  "name": "o-apocalipse-do-delivery",
  "version": "1.0.0",
  "description": "Microsserviço de checkout com testes, BDD, mutação e resiliência.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "test:coverage": "jest --coverage --runInBand",
     "mutation": "stryker run"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/jest-runner": "^9.6.1",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
